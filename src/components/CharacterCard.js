import React from "react";

export default function CharacterCard() {
  return /*<span>todo: character</span>*/(
    {
      // <Card key={item.id}>
      //   <CardBody>
      //     <h3><CardTitle>Name: {item.name}</CardTitle></h3>
      //     <CardText>Species: {item.species}</CardText>
      //     <CardText>Status: {item.status}</CardText>
      //     <CardText>Origin: {item.origin.name}</CardText>
      //   </CardBody>
      // </Card>
    }
  );
}
/* 
<Card key={item.id}>
    <CardBody>
      <h3><CardTitle>Name: {item.name}</CardTitle></h3>
      <CardText>Species: {item.species}</CardText>
      <CardText>Status: {item.status}</CardText>
      <CardText>Origin: {item.origin.name}</CardText>
    </CardBody>
</Card>
*/